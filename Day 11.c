#include <stdio.h>
#include <stdlib.h>

int main()
{
    int T,N;
    printf("Enter the no of text cases=");
    scanf("%d",&T);
 while(T--)
    {
        printf("Enter the weight of pulp =");
        scanf("%d",&N);
        printf("The no of notebooks=%d\n",N*10);
    }

}
